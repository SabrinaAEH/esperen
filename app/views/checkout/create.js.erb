var stripe = Stripe(' <%= Rails.congiguration.stripe[:publishable_key] %>');
stripe.redirectToCheckout({
    sessionId: '<%= session.id %>'
}).then(function (result){
console.log(result.error.message);
});
